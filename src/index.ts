// recommended to export your plugin as default
// - tests run out of the box
// - auto generation of the config is easier
// - increases DX because there is one way to import plugins
export { plugin as default } from "./plugin.js";
